{
  "name": "worker",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev index.ts",
    "build": "esbuild index.ts --bundle --outfile=dist/worker.js --format=esm --platform=neutral --target=es2022",
    "deploy": "wrangler deploy index.ts",
    "test": "vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "dependencies": {
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260421.1",
    "vitest": "^4.1.5",
    "wrangler": "^4.84.1"
  }
}
