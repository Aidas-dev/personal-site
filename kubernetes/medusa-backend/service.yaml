apiVersion: v1
kind: Service
metadata:
  name: medusa-backend
  namespace: riedu-eshop
  labels:
    app: medusa-backend
    component: backend
spec:
  selector:
    app: medusa-backend
  ports:
    - port: 9000
      targetPort: 9000
      name: http
  type: ClusterIP
